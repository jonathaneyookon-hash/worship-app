const $=id=>document.getElementById(id);function render(s){const a=s?.schedule||[];const i=a.findIndex(x=>x.id===s?.live?.id);const cur=s?.live,nxt=i>=0?a[i+1]:a[0];$('current').textContent=cur?.reference||cur?.title||'Nothing on air';$('currentText').textContent=cur?.text||'';$('next').textContent=nxt?.reference||nxt?.title||'—';$('nextText').textContent=nxt?.text||'';}window.scriptureAPI.onLiveDisplay(render);window.scriptureAPI.onStateUpdate(render);setInterval(()=>$('clock').textContent=new Date().toLocaleTimeString(),1000);